function openTab(evt, tabName) {
    var i, tabcontent, tablinks;
    tabcontent = document.getElementsByClassName("tabcontent");
    for (i = 0; i < tabcontent.length; i++) {
        tabcontent[i].style.display = "none";
    }
    tablinks = document.getElementsByClassName("tablink");
    for (i = 0; i < tablinks.length; i++) {
        tablinks[i].className = tablinks[i].className.replace(" active", "");
    }
    document.getElementById(tabName).style.display = "block";
    evt.currentTarget.className += " active";
  }
  
  // Get the first tab and click it as default
  document.getElementsByClassName("tablink")[0].click();
  
  // Extend the script to handle picture changes and deletes, updating account settings, and toggling switches
  // This will require further JavaScript coding to interact with the backend or further UI adjustments.
  